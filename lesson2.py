import hashlib

pass_hash = input("Enter md5 hash: ")

'''import random
import pyautogui


chars = "abcdefghijklmnopqrstuvwxyz0123456789"
chars_list = list(chars)
password = pyautogui.password("Enter password : ")

guess_password = ""

while (guess_password != password):
    guess_password = random.choice(chars_list, k=len(password))
    
    print("<==========="+ str(guess_password)+ "===========")'''
# its so strange how the same face can make feel so right but bring you so much pain, its so strange how the same face can make love until it hurts
